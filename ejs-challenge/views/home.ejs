<%- include("partials/header") -%> 
    <h1>Home</h1>
    <p><%= content %></p>
   <% post.forEach(element => { %>
    <%= console.log(element.title) %> 
   <% }) %>
<%- include("partials/footer") %> 